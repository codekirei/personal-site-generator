extends /base.jade

block head
  title!= data.title
  meta(name='description', content!= data.description)

block banner
  h1.banner__title!= data.title

block content
  article.post(role='article')!= data.content
    img.fleuron(src='/img/kunai-fleuron.png', alt='fleuron')
  include /partials/post-author.jade
