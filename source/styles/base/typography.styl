{$h}
  opensans(bold)
  font-weight: normal
  line-height: 1

{$text}
  opensans()

p
  max-width: $linewrap
  line-height: 1.8

.index-post
  &__read-button
  &__tag
    opensans(bold)

//------------------------------------------------------------------------------
// font sizes
//------------------------------------------------------------------------------
{$text}
  fs: 18px
  +above($bp)
    fs: 20px

// index
.index-banner
  &__title
    fs: 50px
    +above($bp)
      fs: 100px
  &__description
    fs: 30px
    +above($bp)
      fs: 60px
.index-post
  &__title
    fs: 30px
  &__read-button
  &__tag
    fs: 16px

// post
.post-banner__title
  fs: 80px
.author
  &__title
    fs: 26px
  &__name
    opensans(bold)
    fs: 40px
