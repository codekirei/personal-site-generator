$bp = 760px
$linewrap = 900px
$gap = 40px
$gap2 = round($gap / 2)
$gap4 = round($gap / 4)

// colors
//------------------------------------------------------------------------------
$c = {
  prime: #B11111, // primary
  acc: #fff,      // accent
  lg: #eee,       // light gray
  gray: #aaa,     // gray
  dg: #555,       // dark gray
  text: #222
}
$c[primeHover] = darken($c.prime, 50%)
